# After installing deps: pip install -e .  (see README — required for imports)

# ─── Core API ────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.10.6          # 2.6.x pydantic-core often lacks cp313 wheels (needs Rust)

# ─── Database ────────────────────────────────
sqlalchemy==2.0.36        # 2.0.29 breaks on Python 3.13 (TypingOnly / __static_attributes__)
aiosqlite==0.20.0          # async SQLite driver
psycopg2-binary==2.9.10    # PostgreSQL driver (2.9.10+ has cp313 wheels on Windows)
alembic==1.13.1            # database migrations

# ─── Redis ───────────────────────────────────
redis==5.0.3

# ─── ML ──────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.5.2        # 1.4.1 has no cp313 wheels
shap==0.48.0               # feature importance / explainability
pandas==2.2.3
numpy==2.2.2               # 1.26.x has no cp313 wheels; pairs with mlflow 2.18+

# ─── MLflow ──────────────────────────────────
mlflow==2.18.0             # 2.11.x pins numpy<2 (conflicts with numpy 2 on 3.13)

# ─── Data Generation ─────────────────────────
faker==24.3.0

# ─── Drift Detection ─────────────────────────
evidently==0.4.22

# ─── Terminal UI ─────────────────────────────
rich==13.7.1               # beautiful terminal output

# ─── Rate limiting ───────────────────────────
slowapi==0.1.9             # per-IP rate limiting for FastAPI

# ─── Testing ─────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0          # coverage measurement + CI gate
httpx==0.28.1              # async HTTP client for API tests
schemathesis==3.28.0       # OpenAPI contract testing (property-based)
factory-boy==3.3.3         # test object factories (TransactionFactory, UserFactory)
locust==2.46.0             # load testing (tests/locustfile.py)
hypothesis==6.157.0        # property-based testing (tests/test_scorer_properties.py)

# ─── Data validation ─────────────────────────
pandera==0.21.1            # DataFrame schema validation; 0.21+ required for NumPy 2.0

# ─── Observability ───────────────────────────
prometheus-client==0.20.0  # /metrics endpoint + counters/histograms
opentelemetry-sdk==1.41.1                      # OTel core SDK
opentelemetry-instrumentation-fastapi==0.62b1  # auto-instrument FastAPI routes
opentelemetry-instrumentation-sqlalchemy==0.62b1  # auto-instrument SQLAlchemy queries
opentelemetry-exporter-otlp-proto-grpc==1.41.1  # OTLP gRPC exporter → Jaeger / collector

# ─── Utilities ───────────────────────────────
python-json-logger==2.0.7
typer==0.12.3              # CLI for the scripts
honcho==2.0.0              # dev: run all processes from Procfile with one command


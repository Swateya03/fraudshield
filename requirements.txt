# After installing deps: pip install -e .  (see README — required for imports)

# ─── Core API ────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.10.6          # 2.6.x pydantic-core often lacks cp313 wheels (needs Rust)

# ─── Database ────────────────────────────────
sqlalchemy==2.0.36        # 2.0.29 breaks on Python 3.13 (TypingOnly / __static_attributes__)
aiosqlite==0.20.0          # async SQLite driver
psycopg2-binary==2.9.10    # PostgreSQL driver (2.9.10+ has cp313 wheels on Windows)
alembic==1.13.1            # database migrations

# ─── Redis ───────────────────────────────────
redis==5.0.3

# ─── ML ──────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.5.2        # 1.4.1 has no cp313 wheels
shap==0.48.0               # feature importance / explainability
pandas==2.2.3
numpy==2.2.2               # 1.26.x has no cp313 wheels; pairs with mlflow 2.18+

# ─── MLflow ──────────────────────────────────
mlflow==2.18.0             # 2.11.x pins numpy<2 (conflicts with numpy 2 on 3.13)

# ─── Data Generation ─────────────────────────
faker==24.3.0

# ─── Drift Detection ─────────────────────────
evidently==0.4.22

# ─── Terminal UI ─────────────────────────────
rich==13.7.1               # beautiful terminal output

# ─── Testing ─────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
httpx==0.27.0              # async HTTP client for API tests

# ─── Utilities ───────────────────────────────
python-json-logger==2.0.7
typer==0.12.3              # CLI for the scripts

